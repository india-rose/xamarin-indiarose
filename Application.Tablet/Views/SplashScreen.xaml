<mvvm:MvvmPage
    x:Class="Application.Tablet.Views.SplashScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Application.Tablet.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Storm.Mvvm"
    mc:Ignorable="d">
    <!--
    DataContext="{Binding Source={StaticResource Locator}, Path=SplashScreenViewModel}"-->

    <Grid VerticalAlignment="Stretch" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"></ColumnDefinition>
            <ColumnDefinition Width="100*"></ColumnDefinition>
            <ColumnDefinition Width="50*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"></RowDefinition>
            <RowDefinition Height="75*"></RowDefinition>
            <RowDefinition Height="20*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <Image Source="../../Assets/logoIndiaRose.png" Grid.Row="1"
               Grid.Column="1 " />

        <TextBlock Grid.Row="2" Grid.Column="1" Text="votre texte ici" 
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

    </Grid>
</mvvm:MvvmPage>
